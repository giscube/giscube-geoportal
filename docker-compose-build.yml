version: "3.8"

services:
  app:
    build: .
    volumes:
      - .:/app
      - geoportal-github-quasar1:/app/node_modules
    environment:
      - LIB=True
      - NODE_ENV=development
    command: bash -c "quasar build"

volumes:
  geoportal-github-quasar1:
